import { DrawPolyRectangle } from './draw-poly-rectangle';

describe('DrawPolyRectangle', () => {
  it('should create an instance', () => {
    expect(new DrawPolyRectangle()).toBeTruthy();
  });
});
