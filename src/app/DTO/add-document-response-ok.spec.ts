import { AddDocumentResponseOk } from './add-document-response-ok';

describe('AddDocumentResponseOk', () => {
  it('should create an instance', () => {
    expect(new AddDocumentResponseOk()).toBeTruthy();
  });
});
