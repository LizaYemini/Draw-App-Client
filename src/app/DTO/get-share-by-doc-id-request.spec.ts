import { GetShareByDocIdRequest } from './get-share-by-doc-id-request';

describe('GetShareByDocIdRequest', () => {
  it('should create an instance', () => {
    expect(new GetShareByDocIdRequest()).toBeTruthy();
  });
});
