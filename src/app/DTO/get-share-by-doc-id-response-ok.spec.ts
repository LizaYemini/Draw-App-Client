import { GetShareByDocIdResponseOk } from './get-share-by-doc-id-response-ok';

describe('GetShareByDocIdResponseOk', () => {
  it('should create an instance', () => {
    expect(new GetShareByDocIdResponseOk()).toBeTruthy();
  });
});
