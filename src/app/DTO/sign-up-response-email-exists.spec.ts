import { SignUpResponseEmailExists } from './sign-up-response-email-exists';

describe('SignUpResponseEmailExists', () => {
  it('should create an instance', () => {
    expect(new SignUpResponseEmailExists()).toBeTruthy();
  });
});
