import { ValidateIdRequest } from './validate-id-request';

describe('ValidateIdRequest', () => {
  it('should create an instance', () => {
    expect(new ValidateIdRequest()).toBeTruthy();
  });
});
